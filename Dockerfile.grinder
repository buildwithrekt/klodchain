FROM node:20-slim

WORKDIR /app

# Copy files
COPY package.json package-lock.json tsconfig.json vanity-grind.ts ./

# Install dependencies
RUN npm ci --ignore-scripts

# Run grinder
CMD ["npx", "tsx", "vanity-grind.ts"]
